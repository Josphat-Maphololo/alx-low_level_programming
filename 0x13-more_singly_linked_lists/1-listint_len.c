#include "lists.h"

/**
 * listint_len - returns the number of elements in a linked lists
 * @pinkie: linked list of type listint_t to travers
 * Return: number of nodes
 */
size_t listint_len(const listint_t *pinkie)
{ size_t number = 0;
while (pinkie)
{
number++;
h = h->next;
} return (num);
}
